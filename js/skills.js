const widthOfCol = $(".skill").width();

const skillImg = $(".skill-figure");

const lanWidth = $("#lang").width();

let margin;
let padding = 0;



// if(screen.width >= 992) {
//   margin = (lanWidth - widthOfCol * 4) / 8;
// } else if(screen.width >= 768){
//   margin = (lanWidth - widthOfCol * 7) / 14;
// } else if(screen.width >= 576) {
//   margin = (lanWidth - widthOfCol * 4) / 8;
// } else {
//   margin = (lanWidth - widthOfCol * 3) / 6;
// }
// if(margin<2){
//   padding = 10;
// }
// skillImg.css({width: widthOfCol-10, height: 7/6*widthOfCol});
// $('.skill').css({margin: margin, padding: padding})